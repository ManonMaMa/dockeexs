version: '3'
services:
  web:
    # même utilité que la commande build
    build: .
    ports:
      - "5050:5000"
    depends_on:
      - db
    environment:
      - DATABASE_URL=postgresql://pokemon:password@db/myapp
    # ce qui permet de recharger la page avec les modifs de la base de données
    volumes:
      - .:/app
  db:
    image: postgres
    environment:
      - POSTGRES_USER=pokemon
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=myapp
    # sauvegarder les données
    volumes: 
      - postgres_data:/var/lib/postgresql/data
# ________permet d'avoir un œil sur la bd________
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

# récupère les données de la bd
volumes:
  postgres_data: